<!-- 70966294-99f0-46ed-9774-d7a021902b8d b34dc407-a54b-43be-9966-7e032b9f5f0c -->
# Plan

1. **Start an interactive rebase covering the two commits** – run `git rebase -i 2fd2abfc^` so the rebase todo list shows `2fd2abfc` followed by `b5b186c7`.
2. **Squash the earlier commit into the later one** – mark `2fd2abfc` as `squash` (keeping `b5b186c7` as pick) and let the combined commit message default to the most recent description.
3. **Finish the rebase and verify history** – resolve any conflicts if they appear, run `git status` to ensure a clean tree, then `git log -n 5 --oneline` to confirm there is one combined commit at the top with the expected message.

No file edits yet; waiting for approval to execute these steps.

### To-dos

- [ ] Confirm ally tracking
- [ ] Restrict packet distribution
- [ ] Limit notif sound to allies
- [ ] Confirm client marker handling unaffected