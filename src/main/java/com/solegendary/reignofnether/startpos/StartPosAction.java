package com.solegendary.reignofnether.startpos;

public enum StartPosAction {
    ADD,
    REMOVE,
    RESERVE,
    UNRESERVE,
    RESET,
    SET_GAME_STARTING,
    UNSET_GAME_STARTING,
    TOGGLE_READY,
    READY_CHECK_STARTED,
    READY_CHECK_CANCELLED
}
