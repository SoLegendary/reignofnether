package com.solegendary.reignofnether.hud;

public class Button {
    int x1; // top left
    int y1;
    int x2; // bottom right
    int y2;

    boolean selected;

    /*
    * create an array of Button classes, then in HudClientEvents write up MouseEvents to cover on click and on hover events
    *
    * change onDrawScreen() to instead render based on the list of buttons' coordinates
    *
    * keep a consistent texture to render, with size based on the x/y coords
    */

    // callback: on click

    // callback: on hover

    // render tooltip (on hover)

    // render texture (unselected)
    // render texture (selected)

}
